export class ApControl {
    constructor(element: HTMLElement, options?: any) {
        this.element = element;
        this.options = options;
    }
    private element: HTMLElement;
    private options: any;
}